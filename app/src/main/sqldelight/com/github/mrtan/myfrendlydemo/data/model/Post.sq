CREATE TAble post (
    _id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    url TEXT NOT NULL,
    height INTEGER AS Integer NULL,
    width INTEGER AS Integer NULL
);

select_all:
SELECT *
FROM post;