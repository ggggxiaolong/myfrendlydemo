CREATE TAble post (
    _id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    title TEXT NOT NULL,
    url TEXT NOT NULL,
    height INTEGER AS Integer NOT NULL,
    width INTEGER AS Integer NOT NULL
);

select_all:
SELECT *
FROM post;